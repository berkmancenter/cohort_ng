<% unless tag_list.blank? %>
  <span class="tagMatches existingTags">
    <% tag_list.each do |t| %>
      <span id="tag_<%= t.id %>"><%= t.hierarchical_name(' :: ') %></span>
    <% end %>
  </span>
  <div class="clear"></div>
<% end %>

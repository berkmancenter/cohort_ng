<% unless @notes.blank? %>
  <div class="notes">
  <%= render :partial => 'shared/lines/note', :collection => @notes %>
  <div class="clear"></div>
  <%= will_paginate(@notes) %>
  </div>
<% else %>
  <h2>None found.</h2>
<% end %>

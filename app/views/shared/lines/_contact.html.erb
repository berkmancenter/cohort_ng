<li id="contact-<%= contact.id %>"><%= link_to(contact, contact_path(contact), :class => 'dialog-show') %> <span class="floating-control control-contact-<%= contact.id %>"> <%= link_to('add doc',new_document_path(:contact_id => contact.id),:class => 'dialog-form') %><%= link_to('add note',new_note_path(:contact_id => contact.id),:class => 'dialog-form') %> <%= link_to('edit',edit_contact_path(contact),:class => 'dialog-form') %> <%= link_to('del',contact_path(contact), :method => :delete, :class => 'delete delete-contact') %></span></li>

<div class="email<%= (email.is_primary) ? ' primary' : '' %>">
  <%= mail_to(email.email) %>, <%= email.email_type unless email.email_type == 'unknown' %> <%= raw (email.is_primary) ? ', <strong>primary</strong>' : '' %>
</div>
